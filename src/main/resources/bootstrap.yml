spring:
 config:
   name: "authentication-service"
 application:
   name: "authentication-service"
 cloud:
   config:
     failFast: true
     discovery:
       enabled: true


eureka:
 client:
   serviceUrl:
     defaultZone: http://discovery-service:8761/eureka/
 instance:
  preferIpAddress: true