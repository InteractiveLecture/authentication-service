spring:
 config:
   name: "gateway-service"
 application:
   name: "gateway-service"
 cloud:
   config:
     failFast: true
     discovery:
       enabled: true


eureka:
 client:
   serviceUrl:
     defaultZone: http://localhost:8761/eureka/